{{ 'component-slideshow.css' | asset_url | stylesheet_tag }}
{{ 'component-slider.css' | asset_url | stylesheet_tag }}

<style>
  /* Hide arrow buttons in announcement bar */
  .announcement-bar .slider-button--prev,
  .announcement-bar .slider-button--next {
    display: none !important;
    visibility: hidden !important;
    opacity: 0 !important;
    width: 0 !important;
    height: 0 !important;
    overflow: hidden !important;
  }

  /* Sticky announcement bar - positioned below moving announcement */
  body:has(.moving-announcement) .announcement-bar-section {
    position: sticky !important;
    top: var(--moving-announcement-height, 0) !important;
    z-index: 9998 !important;
    background-color: rgb(var(--color-background)) !important;
    margin: 0 !important;
    margin-top: 0 !important;
    margin-bottom: 0 !important;
    padding: 0 !important;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
  }

  /* Remove section spacing */
  body:has(.moving-announcement) section.announcement-bar-section {
    margin: 0 !important;
    padding: 0 !important;
  }

  /* When no moving announcement, keep normal positioning */
  .announcement-bar-section:not(:has(+ .moving-announcement)) {
    position: static !important;
  }

  /* Ensure proper height and no cropping */
  body:has(.moving-announcement) .utility-bar,
  body:has(.moving-announcement) .announcement-bar-section .utility-bar {
    min-height: 56px !important;
    padding: 0 !important;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
    margin: 0 !important;
    margin-top: 0 !important;
    margin-bottom: 0 !important;
    overflow: visible !important;
    line-height: 1 !important;
    border: none !important;
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
  }

  @media screen and (max-width: 749px) {
    body:has(.moving-announcement) .utility-bar,
    body:has(.moving-announcement) .announcement-bar-section .utility-bar {
      min-height: 48px !important;
    }
  }

  /* Remove all spacing from utility-bar__grid */
  body:has(.moving-announcement) .utility-bar__grid {
    margin: 0 !important;
    padding: 0 !important;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
    line-height: 1 !important;
    max-width: 100% !important;
  }
  
  /* Override page-width padding ONLY for header group elements */
  body:has(.moving-announcement) .shopify-section-group-header-group .page-width {
    padding-left: 0 !important;
    padding-right: 0 !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
  }

  /* Remove border from utility bar */
  body:has(.moving-announcement) .utility-bar--bottom-border,
  body:has(.moving-announcement) .utility-bar--bottom-border-social-only {
    border-bottom: none !important;
    border-top: none !important;
  }

  /* Remove spacing after announcement bar section */
  body:has(.moving-announcement) .announcement-bar-section {
    margin-bottom: 0 !important;
    padding-bottom: 0 !important;
  }

  /* Remove spacing from page-width container */
  body:has(.moving-announcement) .announcement-bar-section .page-width {
    margin: 0 !important;
    padding: 0 !important;
    padding-top: 0 !important;
    padding-bottom: 0 !important;
  }

  .announcement-bar__message {
    margin: 0 !important;
    padding: 0 !important;
    line-height: 1 !important;
    white-space: normal !important;
    word-wrap: break-word !important;
  }

  /* Remove all spacing from announcement bar container and center it */
  body:has(.moving-announcement) .announcement-bar {
    margin: 0 auto !important;
    padding: 0 !important;
    line-height: 1 !important;
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
    width: 100% !important;
  }

  /* Center the grid item containing announcement bar */
  body:has(.moving-announcement) .utility-bar__grid {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    width: 100% !important;
    max-width: 100% !important;
  }

  /* Ensure announcement bar grid item is centered */
  body:has(.moving-announcement) .utility-bar__grid > .announcement-bar {
    flex: 1 !important;
    justify-self: center !important;
    margin: 0 auto !important;
    width: 100% !important;
    display: flex !important;
    justify-content: center !important;
  }

  /* Center the announcement bar container */
  body:has(.moving-announcement) .announcement-bar {
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
    width: 100% !important;
    margin: 0 auto !important;
    padding: 0 !important;
  }

  /* Display one point at a time with rotation */
  .announcement-bar__points {
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
    padding: 1rem 0 !important;
    margin: 0 auto !important;
    min-height: 3.5rem;
    position: relative;
    overflow: hidden;
    line-height: 1.4 !important;
    width: 100% !important;
    max-width: 100% !important;
  }

  .announcement-bar__point {
    display: none !important;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-size: 1.3rem;
    font-weight: 500;
    color: rgb(var(--color-foreground));
    white-space: nowrap;
    opacity: 0;
    transition: opacity 0.5s ease-in-out;
    margin: 0 auto !important;
    padding: 0 !important;
    line-height: 1 !important;
    width: auto !important;
    max-width: 100% !important;
    text-align: center !important;
  }

  .announcement-bar__point.active {
    display: flex !important;
    opacity: 1 !important;
    margin: 0 auto !important;
    justify-content: center !important;
  }

  .announcement-bar__point::before {
    content: '✓';
    color: #6CA93A;
    font-weight: bold;
    font-size: 1.4rem;
  }

  @media screen and (max-width: 749px) {
    body:has(.moving-announcement) .utility-bar {
      padding: 0 !important;
      margin: 0 !important;
      margin-top: 0 !important;
      margin-bottom: 0 !important;
      width: 100% !important;
      max-width: 100vw !important;
      overflow-x: hidden !important;
      box-sizing: border-box !important;
    }

    body:has(.moving-announcement) .announcement-bar-section {
      margin: 0 !important;
      margin-top: 0 !important;
      margin-bottom: 0 !important;
      padding: 0 !important;
      padding-top: 0 !important;
      padding-bottom: 0 !important;
      width: 100% !important;
      max-width: 100vw !important;
      overflow-x: hidden !important;
      box-sizing: border-box !important;
    }

    body:has(.moving-announcement) .announcement-bar {
      display: flex !important;
      justify-content: center !important;
      align-items: center !important;
      width: 100% !important;
      max-width: 100vw !important;
      margin: 0 auto !important;
      padding: 0 !important;
      overflow-x: hidden !important;
      box-sizing: border-box !important;
    }

    body:has(.moving-announcement) .shopify-section-group-header-group .announcement-bar-section .page-width {
      margin: 0 !important;
      padding: 0 !important;
      padding-top: 0 !important;
      padding-bottom: 0 !important;
      max-width: 100vw !important;
      overflow-x: hidden !important;
      box-sizing: border-box !important;
    }

    .announcement-bar__points {
      min-height: 3rem;
      padding: 0.8rem 0 !important;
      margin: 0 auto !important;
      width: 100% !important;
      max-width: 100vw !important;
      justify-content: center !important;
      display: flex !important;
      overflow-x: hidden !important;
      box-sizing: border-box !important;
    }

    .announcement-bar__point {
      font-size: 1.1rem;
      line-height: 1 !important;
      width: auto !important;
      max-width: 90vw !important;
      text-align: center !important;
      justify-content: center !important;
      margin: 0 auto !important;
    }

    .announcement-bar__point.active {
      display: flex !important;
      justify-content: center !important;
      margin: 0 auto !important;
    }

    /* Remove spacing between announcement bar and header on mobile */
    body:has(.moving-announcement) .announcement-bar-section + .section-header,
    body:has(.moving-announcement) .announcement-bar-section + section[class*="header"] {
      margin: 0 !important;
      margin-top: 0 !important;
      margin-bottom: 0 !important;
      padding: 0 !important;
      padding-top: 0 !important;
      padding-bottom: 0 !important;
    }
  }
</style>

{%- liquid
  assign social_icons = true
  if settings.social_facebook_link == blank and settings.social_instagram_link == blank and settings.social_youtube_link == blank and settings.social_tiktok_link == blank and settings.social_twitter_link == blank and settings.social_pinterest_link == blank and settings.social_snapchat_link == blank and settings.social_tumblr_link == blank and settings.social_vimeo_link == blank
    assign social_icons = false
  endif
  if section.settings.enable_country_selector or section.settings.enable_language_selector
    assign language_country_selector = true
  endif
  if section.blocks.size > 0
    assign announcement_bar = true
  endif
-%}

{% if social_icons %}
  {{ 'component-list-social.css' | asset_url | stylesheet_tag }}
{% endif %}

<div
  class="utility-bar color-{{ section.settings.color_scheme }} gradient{% if section.settings.show_line_separator and section.blocks.size > 0 %} utility-bar--bottom-border{% elsif section.settings.show_line_separator and section.settings.show_social and social_icons%} utility-bar--bottom-border-social-only{% endif %}{% if section.settings.enable_country_selector or section.settings.enable_language_selector %} header-localization{% endif %}"
>
  <div class="page-width utility-bar__grid{% if announcement_bar and language_country_selector or section.settings.show_social and social_icons %} utility-bar__grid--3-col{% elsif language_country_selector or section.settings.show_social and social_icons %} utility-bar__grid--2-col{% endif %}">
    {%- if section.settings.show_social and social_icons -%}
      {%- render 'social-icons' -%}
    {%- endif -%}
    {%- if section.blocks.size == 1 -%}
      <div
        class="announcement-bar{% if section.settings.show_social %} announcement-bar--one-announcement{% endif %}"
        role="region"
        aria-label="{{ 'sections.header.announcement' | t }}"
        {{ section.blocks.first.shopify_attributes }}
      >
        {%- if section.blocks.first.settings.text != blank -%}
          {%- if section.blocks.first.settings.link != blank -%}
            <a
              href="{{ section.blocks.first.settings.link }}"
              class="announcement-bar__link link link--text focus-inset animate-arrow"
            >
          {%- endif -%}
          <p class="announcement-bar__message h5">
            <span>{{ section.blocks.first.settings.text | escape }}</span>
            {%- if section.blocks.first.settings.link != blank -%}
              {{- 'icon-arrow.svg' | inline_asset_content -}}
            {%- endif -%}
          </p>
          {%- if section.blocks.first.settings.link != blank -%}
            </a>
          {%- endif -%}
        {%- endif -%}
      </div>
    {%- elsif section.blocks.size > 1 -%}
      <!-- Display blocks one at a time with rotation -->
      <div
        class="announcement-bar{% if section.settings.show_social %} announcement-bar--one-announcement{% endif %}"
        role="region"
        aria-label="{{ 'sections.header.announcement' | t }}"
      >
        <div class="announcement-bar__points" data-announcement-rotator>
          {%- for block in section.blocks -%}
            <div class="announcement-bar__point{% if forloop.first %} active{% endif %}" {{ block.shopify_attributes }}>
              {{ block.settings.text | escape }}
            </div>
          {%- endfor -%}
        </div>
      </div>
    {%- else -%}
      <!-- Default 3 points if no blocks - show one at a time -->
      <div
        class="announcement-bar{% if section.settings.show_social %} announcement-bar--one-announcement{% endif %}"
        role="region"
        aria-label="{{ 'sections.header.announcement' | t }}"
      >
        <div class="announcement-bar__points" data-announcement-rotator>
          <div class="announcement-bar__point active">Advanced Netting for Modern Agriculture</div>
          <div class="announcement-bar__point">Chemical-Free Netting Solutions for Sustainable Agriculture</div>
          <div class="announcement-bar__point">Designed to Protect. Built to Last.</div>
        </div>
      </div>
      {%- if request.design_mode -%}
        <script src="{{ 'theme-editor.js' | asset_url }}" defer="defer"></script>
        <style>
          /* theme editor power preview fix */
          .announcement-bar-slider .slider__slide[aria-hidden='true'] {
            visibility: hidden;
          }
        </style>
      {%- endif -%}
    {%- endif -%}
    <div class="localization-wrapper">
      {%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
        <localization-form class="small-hide medium-hide">
          {%- form 'localization', id: 'AnnouncementCountryForm', class: 'localization-form' -%}
            <div>
              <h2 class="visually-hidden" id="AnnouncementCountryLabel">{{ 'localization.country_label' | t }}</h2>
              {%- render 'country-localization', localPosition: 'AnnouncementCountry' -%}
            </div>
          {%- endform -%}
        </localization-form>
      {% endif %}

      {%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
        <localization-form class="small-hide medium-hide">
          {%- form 'localization', id: 'AnnouncementLanguageForm', class: 'localization-form' -%}
            <div>
              <h2 class="visually-hidden" id="AnnouncementLanguageLabel">{{ 'localization.language_label' | t }}</h2>
              {%- render 'language-localization', localPosition: 'AnnouncementLanguage' -%}
            </div>
          {%- endform -%}
        </localization-form>
      {%- endif -%}
    </div>
  </div>
</div>

<script>
  (function() {
    const rotator = document.querySelector('[data-announcement-rotator]');
    if (!rotator) return;

    const points = rotator.querySelectorAll('.announcement-bar__point');
    if (points.length <= 1) return;

    let currentIndex = 0;
    const rotationSpeed = 4000; // 4 seconds

    function rotateAnnouncement() {
      // Remove active class from current point
      points[currentIndex].classList.remove('active');
      
      // Move to next point
      currentIndex = (currentIndex + 1) % points.length;
      
      // Add active class to new point
      points[currentIndex].classList.add('active');
    }

    // Start rotation
    const intervalId = setInterval(rotateAnnouncement, rotationSpeed);

    // Cleanup on page unload
    window.addEventListener('beforeunload', function() {
      clearInterval(intervalId);
    });
  })();
</script>

{% schema %}
{
  "name": "t:sections.announcement-bar.name",
  "max_blocks": 12,
  "class": "announcement-bar-section",
  "enabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "checkbox",
      "id": "auto_rotate",
      "label": "t:sections.announcement-bar.settings.auto_rotate.label",
      "default": false
    },
    {
      "type": "range",
      "id": "change_slides_speed",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "label": "t:sections.announcement-bar.settings.change_slides_speed.label",
      "default": 5
    },    
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-4"
    },  
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    },
    {
      "type": "header",
      "content": "t:sections.announcement-bar.settings.heading_utilities.content"
    },
    {
      "type": "paragraph",
         "content": "t:sections.announcement-bar.settings.paragraph.content"
    },             
    {
      "type": "checkbox",
      "id": "show_social",
      "default": false,
      "label": "t:sections.announcement-bar.settings.show_social.label",
      "info": "t:sections.announcement-bar.settings.show_social.info"
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "default": false,
      "label": "t:sections.announcement-bar.settings.enable_country_selector.label",
      "info": "t:sections.announcement-bar.settings.enable_country_selector.info"
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "default": false,
      "label": "t:sections.announcement-bar.settings.enable_language_selector.label",
      "info": "t:sections.announcement-bar.settings.enable_language_selector.info"
    }
  ],
  "blocks": [
    {
      "type": "announcement",
      "name": "t:sections.announcement-bar.blocks.announcement.name",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "default": "t:sections.announcement-bar.blocks.announcement.settings.text.default",
          "label": "t:sections.announcement-bar.blocks.announcement.settings.text.label"
        },
        {
          "type": "url",
          "id": "link",
          "label": "t:sections.announcement-bar.blocks.announcement.settings.link.label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "t:sections.announcement-bar.presets.name",
      "blocks": [
        {
          "type": "announcement"
        }
      ]
    }
  ]
}
{% endschema %}
